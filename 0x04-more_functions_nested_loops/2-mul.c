#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 * @a: first num to be multiply
 * @b: second num to be multiply
 * Return: 1 if num is a digit, 0 otherwise
 */
int mul(int a, int b)
{
	return (a * b);
}
